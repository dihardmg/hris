-- Business travel city column was already fixed by migration V30
-- This migration is now a no-op since the schema was already updated
-- The business_travel_requests table already has the proper city_id column and constraints
SELECT 1;